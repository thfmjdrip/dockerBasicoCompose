# DOCKER 
subi um site simples em um servidor apache para testar meus conhecimentos de doocker-compose(networks,volumes,etc...)
# PARA RODAR
tenha em seu pc o docker e docker-compose(alguns pode virar separados),de um clone no repositorio git clone: git@github.com:thfmjdrip/dockerBasicoCompose.git
na mesma pasta entre no seu terminal/shell/(seu  modo de usar o cmd) e digite docker-compose up -d 
o -d e para rodar em background
